# Backend Application Deployment

## Overview

This backend application is part of the AWS Three-Tier Architecture project. The application is built using Python and Flask and runs on an EC2 instance in a private application subnet.

## Requirements

- Python 3
- pip3
- Flask
- Git

## Install Dependencies

```bash
pip3 install -r requirements.txt
